# 后续科研优化方向与执行方案整理

## 1. 当前阶段判断

当前工作已经完成了低保真 synthetic 数据上的 baseline 闭环，包括数据生成、传统机器学习模型、TabPFN-2.5 基线、统一指标、结果合并与初步可视化。现有结果说明 TabPFN-2.5 在当前低保真解析任务上具备较强的数据效率和预测能力，可以作为后续 low-to-high fidelity adaptation 的核心 backbone。

但当前结果仍属于前置验证。后续真正的创新点应集中在：如何从大量低保真 synthetic universe 中选择对高保真 FEA target domain 更有迁移价值的数据，并将其用于 TabPFN / TabPFN-2.5 的 context construction、adaptation 或 few-shot tuning。

后续研究主线建议固定为：

> 面向高保真工程小样本表格回归任务，构建工程语义约束的 low-fidelity synthetic subset selection 方法，并验证其对 Tabular Foundation Model 领域适配的增益。

---

## 2. 后续优化方向总览

1. 扩展 LHS/Sobol 空间采样  
2. 增加临界密封样本过采样  
3. 加入物理可行性过滤  
4. 构建工程语义 subsets  
5. 完善 subset 元信息表  
6. 实现 vanilla OT 选择器  
7. 构建 PIOT cost matrix  
8. 加入 bridge 语义距离  
9. 加入风险特征距离项  
10. 加入物理一致性惩罚  
11. 比较 MMD/CORAL/OT 基线  
12. 优化 Top-K subset 选择  
13. 加入 subset 多样性约束  
14. 设计安全覆盖约束  
15. 尝试 soft subset weighting  
16. 优化 TabPFN context 构造  
17. 对比 full/random/selected synthetic  
18. 做 FEA target-domain 切分  
19. 分析近阈值失败样本  
20. 补充 feature masking 鲁棒性实验  

---

## 3. 方向 A：Synthetic Universe 优化

### 3.1 扩展 LHS / Sobol 空间采样

当前 low-fidelity synthetic 数据主要来自随机采样。后续可引入 Latin Hypercube Sampling 和 Sobol quasi-random sampling，以提升高维输入空间覆盖质量。

建议生成三套数据：

```text
synthetic_random_100k.csv
synthetic_lhs_100k.csv
synthetic_sobol_100k.csv
```

比较内容包括：

```text
1. 输入空间覆盖率
2. target-domain 覆盖率
3. near-threshold 样本比例
4. physical violation ratio
5. 后续 selection / adaptation gain
```

该方向可以支撑的结论是：低保真数据质量不只取决于样本量，也取决于采样策略和工程空间覆盖。

---

### 3.2 增加临界密封样本过采样

当前整体 baseline 中 TabPFN-2.5 表现很强，但 near-threshold 样本可能偏少。后续需要主动构造临界安全边界附近的数据。

建议定义：

```text
D_normal：普通工况
D_critical：near-threshold 工况
D_failure：明显失效工况
D_safe_large_margin：明显安全工况
```

near-threshold 条件可设为：

```text
|seal_margin_proxy| <= 0.5
|seal_margin_proxy| <= 1.0
|seal_margin_proxy| <= 2.0
```

该方向可以支撑的结论是：密封安全预测中，临界样本比大量普通安全样本具有更高训练价值。

---

### 3.3 加入物理可行性过滤

后续 synthetic 数据需要加入物理一致性检查，避免生成工程上明显不合理的样本。

可加入以下过滤规则：

```text
1. 预紧力不超过材料屈服安全范围
2. 摩擦系数处于合理工艺区间
3. 几何参数组合满足基本装配约束
4. 垫片宽度、螺栓圈直径、法兰厚度比例合理
5. p_min_gasket 与 seal_margin 不出现明显异常极值
```

建议为每个样本增加字段：

```text
physical_violation_score
is_physically_valid
```

后续可将 `physical_violation_score` 放入 PIOT cost 中，作为物理一致性惩罚项。

---

## 4. 方向 B：Physical Subset 构造优化

### 4.1 构建工程语义 subsets

后续 subset 构造不宜只按单变量切分，应围绕工程失效机制和安全风险组织。

建议构造如下 subsets：

```text
S1: high pressure + low gasket stiffness
S2: high preload scatter + high friction uncertainty
S3: eccentric load + low contact uniformity
S4: near-threshold seal margin
S5: low clamp reserve
S6: high flange opening proxy
S7: low gasket stiffness + high eccentricity
S8: high pressure + high scatter + near-threshold
```

这些 subsets 应具有清晰的工程含义，用于支撑后续 PIOT-EASS 的 subset-level selection。

---

### 4.2 完善 subset 元信息表

建议建立：

```text
data/low_fidelity_synthetic/subset_table.csv
```

字段包括：

```text
subset_id
subset_name
subset_rule
n_samples
pressure_level
gasket_stiffness_level
preload_scatter_level
eccentricity_level
friction_uncertainty_level
near_threshold_ratio
unsafe_ratio
mean_p_min_gasket
mean_seal_margin
physical_violation_ratio
```

该表的作用是让 selection 结果具备可解释性。后续可以说明 PIOT 为什么选择某些 subsets，以及这些 subsets 与 FEA target domain 的工程关系。

---

### 4.3 比较 sample-level 与 subset-level selection

建议设置以下对照：

```text
1. random sample selection
2. nearest sample selection
3. random subset selection
4. subset-level PIOT selection
```

如果 subset-level PIOT 更稳定，就可以说明工程语义组织对迁移适配有贡献。

---

## 5. 方向 C：PIOT / 打分器优化

### 5.1 从简易打分器升级为 PIOT cost matrix

建议第一版 PIOT cost 固定为：

```text
C_ij =
α d_raw(x_i, x_j)
+ β d_bridge(b_i, b_j)
+ γ d_risk(r_i, r_j)
+ η R_phys(i, j)
```

其中：

```text
d_raw：几何、材料、载荷、工艺原始输入距离
d_bridge：夹紧力、载荷分配、接触均匀性等桥接语义距离
d_risk：seal_margin_proxy、near-threshold、unsafe risk 距离
R_phys：物理不一致惩罚
```

这部分是方法创新的核心，应作为后续 `src/piot_scorer.py` 的主要实现目标。

---

### 5.2 距离项设计优化

不同变量不应统一使用普通欧氏距离。建议分层设计：

```text
raw distance:
数值变量使用 standardized L2 或 Mahalanobis distance
离散变量使用 mismatch penalty

bridge distance:
使用 Mahalanobis distance，捕捉 FKP、FKR、Phi 等变量相关性

risk distance:
比较 seal_margin_proxy 分布、near_threshold_ratio、unsafe_ratio

physical penalty:
使用 physical_violation_score 与单调性不一致惩罚
```

risk distance 建议使用分位数距离：

```text
d_risk = ||Q_S(seal_margin_proxy) - Q_T(seal_margin)||_2
```

分位数可取：

```text
q = [0.05, 0.10, 0.25, 0.50, 0.75]
```

---

### 5.3 完善 selection baseline

selection 方法不应只和 random 对比。建议逐级实现：

```text
Level 0: Full synthetic
Level 1: Random subset
Level 2: Nearest centroid
Level 3: MMD
Level 4: CORAL
Level 5: Vanilla OT raw-only
Level 6: Bridge-only distance
Level 7: PIOT raw + bridge + risk + phys
Level 8: PIOT + value learner
```

这样可以形成清晰的消融链条，增强论文说服力。

---

## 6. 方向 D：Subset 选择策略优化

### 6.1 Top-K subset selection

先实现最基础的 Top-K 选择：

```text
选择 PIOT score 最高的 K 个 subset
K = 1, 2, 3, 5
```

做 K 的消融曲线：

```text
K vs RMSE
K vs false-safe rate
K vs adaptation gain
```

该实验可以回答：选多少个 subset 最合适。

---

### 6.2 加入 subset 多样性约束

只选最相似的 subset 可能导致样本冗余。建议使用：

```text
score_final(S_k) =
score_PIOT(S_k, T)
- λ max similarity(S_k, selected_subsets)
```

这样可以避免选出的 subsets 覆盖区域过窄。

---

### 6.3 设计安全覆盖约束

密封预测任务中，selection 还应覆盖安全关键样本。建议加入约束：

```text
selected subsets 中 near-threshold 样本比例 >= τ1
selected subsets 中 unsafe 样本比例 >= τ2
selected subsets 中 target-domain 覆盖率 >= τ3
```

这可以让 selection 更符合工程安全任务需求。

---

### 6.4 尝试 soft subset weighting

第二阶段可尝试：

```text
w_k = softmax(score_PIOT(S_k, T) / τ)
```

然后从 selected subsets 中按权重采样，构造：

```text
D_adapt = weighted sample from selected subsets
```

soft weighting 可以避免硬性 Top-K 带来的选择不稳定。

---

## 7. 方向 E：TabPFN-2.5 优化方向

### 7.1 优化 context construction

优先从 TabPFN 的使用方式优化，而不是直接改模型内部结构。

可比较：

```text
1. random context
2. nearest context
3. bridge-nearest context
4. risk-nearest context
5. PIOT-selected subset context
6. causal-topology guided context
```

context size 可设置为：

```text
32, 64, 128, 256, 512
```

比较指标：

```text
RMSE
MAE
false-safe rate
unsafe recall
near-threshold MAE
```

---

### 7.2 对比不同 synthetic 使用方式

关键对照包括：

```text
FEA-only TabPFN
FEA + full synthetic
FEA + random subset
FEA + nearest subset
FEA + vanilla OT subset
FEA + PIOT subset
```

核心目标是证明：

```text
full synthetic 可能产生负迁移
PIOT-selected synthetic 能减少负迁移
```

这比单纯证明 TabPFN 比传统模型强更有创新性。

---

### 7.3 feature grouping / feature ordering

建议固定输入特征组织：

```text
Group 1: geometry
Group 2: material
Group 3: process
Group 4: load
Group 5: bridge features
Group 6: risk proxy
```

做以下对照：

```text
root_only
root_plus_bridge
root_plus_bridge_plus_risk
```

该实验可以验证 bridge semantic layer 是否真正有用。

---

### 7.4 feature masking 鲁棒性实验

BERT 式掩码机制可以作为后期增强方向，但不建议作为当前主贡献。

可做：

```text
1. 随机 mask 部分输入特征
2. mask geometry / material / process / load 分组
3. 测试 TabPFN 对缺失字段的鲁棒性
4. 比较 root-only 与 bridge-enhanced 输入的抗扰动能力
```

该方向适合作为 robustness experiment 或 supplementary analysis。

---

## 8. 方向 F：FEA Benchmark 与 Target Domain

### 8.1 FEA 数据回来后的第一步

拿到 FEA 后，不应立即训练模型。建议先做：

```text
1. 字段完整性检查
2. FEA vs synthetic 分布对比
3. 标签合理性检查
4. target-domain 切分
5. FEA-only baseline
```

输出：

```text
fea_sanity_report.md
fea_vs_synthetic_distribution.pdf
target_domain_summary.csv
```

---

### 8.2 定义 high-fidelity target domains

建议优先定义：

```text
T1: high pressure + low gasket stiffness
T2: high preload scatter
T3: high eccentricity
T4: near-threshold seal margin
T5: high friction uncertainty
```

每个 target domain 需要记录：

```text
domain rule
n_samples
unsafe_ratio
seal_margin distribution
synthetic nearest subset
selected PIOT subset
```

这部分会成为后续 PIOT-EASS 的主要实验场景。

---

### 8.3 失败模式分析

建议重点分析：

```text
near-threshold false-safe cases
high-pressure failure cases
high-scatter failure cases
bridge mismatch cases
selected subset negative transfer cases
```

这些分析可以把论文从普通 tabular regression 推进到 engineering safety-aware AI。

---

## 9. 后续实验矩阵建议

### Stage 1：FEA-only baseline

目的：证明高保真 benchmark 有可学习信号。

```text
Models:
Ridge, RF, XGBoost, LightGBM, MLP, TabPFN-2.5

Metrics:
MAE, RMSE, R², false-safe rate, unsafe recall, near-threshold MAE
```

如果 FEA 样本较少，可设置：

```text
n_train = 16, 32, 64, 128, 256
```

---

### Stage 2：Synthetic usefulness

目的：证明 low-fidelity synthetic 数据不能全量无筛选使用。

```text
FEA-only
FEA + full synthetic
FEA + random synthetic subset
FEA + nearest synthetic subset
```

重点观察：

```text
negative transfer rate
false-safe rate
near-threshold MAE
```

---

### Stage 3：PIOT-EASS 主实验

目的：证明 PIOT selection 更有效。

```text
FEA + random subset
FEA + MMD subset
FEA + CORAL subset
FEA + Wasserstein subset
FEA + vanilla OT subset
FEA + bridge distance subset
FEA + PIOT-EASS subset
```

---

### Stage 4：Ablation Study

目的：验证每个模块贡献。

```text
raw-only OT
bridge-only OT
risk-only OT
raw + bridge
raw + bridge + risk
raw + bridge + risk + physical penalty
PIOT + value learner
```

---

### Stage 5：增强实验

目的：补强投稿完整性。

```text
target-conditioned value learner
causal-topology context retrieval
feature masking robustness
negative transfer analysis
failure mode case study
```

---

## 10. 等待 FEA 期间可立即执行的任务

1. 重构 synthetic generator，支持 random / LHS / Sobol  
2. 增加 near-threshold oversampling  
3. 加入 physical_violation_score  
4. 编写 build_physical_subsets.py  
5. 输出 subset_table.csv 和 subset_assignments.csv  
6. 实现 random / nearest / MMD / CORAL / vanilla OT selector  
7. 实现 piot_scorer.py  
8. 用 pseudo-target domains 做 selection sanity check  
9. 准备 load_fea_benchmark.py  
10. 准备 validate_fea_fields.py  
11. 准备 build_target_domains.py  
12. 修改 TabPFN 接口，支持 selected subset context  

---

## 11. 近期优先级排序

### 最高优先级

```text
FEA benchmark 协议
synthetic universe 重构
physical subset construction
PIOT subset selection
```

### 中等优先级

```text
TabPFN context construction
bridge / risk / physics ablation
target-domain failure analysis
```

### 较低优先级

```text
BERT-style masking
复杂 value learner
meta-gradient
深度微调 TabPFN
```

---

## 12. 可向导师汇报的总结表述

目前已经完成低保真 synthetic 数据上的 baseline 闭环，结果证明 TabPFN-2.5 可以作为后续 backbone。后续工作重点将从模型对比转向低高保真迁移数据选择。具体而言，接下来会重构 synthetic universe，引入空间覆盖采样、near-threshold 过采样和物理可行性过滤；随后将低保真样本组织成具有工程含义的 physical subsets；再实现 PIOT-EASS，用 raw features、bridge semantic features、risk features 和 physics penalty 共同计算 source subset 与 FEA target domain 的迁移代价。等高保真 FEA 数据回来后，将比较 full synthetic、random subset、MMD/CORAL/vanilla OT 和 PIOT-EASS selected subsets 对 TabPFN-2.5 适配效果的影响，重点关注 RMSE、near-threshold error 和 false-safe rate 的变化。

---

## 13. 最终判断

后续创新点不应押在“让 TabPFN 模型本身变强”，而应押在：

> 如何用工程语义和物理约束，从 low-fidelity synthetic universe 中选择对 high-fidelity FEA target domain 更有迁移价值的数据，并让 TabPFN-2.5 在小样本、安全关键场景下更稳。

这条路线更符合 EAAI 投稿逻辑，也更容易与单纯工程应用或普通模型对比拉开差异。
